services:
  bloo:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - bloo-data:/app/data
      - ${HOME:-/home}:${HOME:-/home}
    environment:
      - BLOO_DATA_DIR=/app/data
      - BLOO_BROWSE_ROOT=${HOME:-/home}
      - BLOO_HTTP_PORT=3000

volumes:
  bloo-data:
