services:
  bloo:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - bloo-data:/app/data
      - ${BLOO_PROJECTS_PATH:-./test-project}:/projects
    environment:
      - BLOO_DATA_DIR=/app/data
      - BLOO_PROJECTS_ROOT=/projects
      - BLOO_HTTP_PORT=3000

volumes:
  bloo-data:
