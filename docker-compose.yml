services:
  bloo:
    build: .
    stdin_open: true
    ports:
      - "3000:3000"
    volumes:
      - ./test-project:/project
    environment:
      - BLOO_PROJECT_ROOT=/project
      - BLOO_STORAGE_ROOT=/project/.bloo
      - BLOO_HTTP_PORT=3000
